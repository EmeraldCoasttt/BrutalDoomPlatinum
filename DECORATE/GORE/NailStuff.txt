        
		/*
		{
		A_WeaponReady(WRF_ALLOWRELOAD);
		if (CountInv("TossGrenade") == 1) { return state("TossGrenade"); }
		if (CountInv("Taunting") == 1) { return state("Taunt"); }
		if (CountInv("Reloading") == 1) { return state("Reload"); }
		if (CountInv("Unloading") == 1) { return state("Unload"); }
		if (CountInv("Salute1") == 1 || CountInv("Salute2") == 1) { return state("Salute"); }
		if (CountInv("IsRunning") == 1) { return state("CheckSprint"); }
		if (CountInv("ExecuteDownedEnemy") == 1) { return state("DoExecution"); }
		if (CountInv("Kicking") == 1) { return state("DoKick"); }
		if (CountInv("StartDualWield") == 1) { return state("DualWield"); }
		if (CountInv("FiredPrimary") == 1) { return state("Fire"); }
        return state("");
		}
		*/
//Basically a better way of checking for BD actions in a ready state.
		

Actor MarineWeakTracer: MonsterTracer
{
	damage (random(10,10))
	DamageType FriendBullet
	Species "Marines"
}

Actor MarineMinigunTracer: MonsterMinigunTracer
{
	damage (random(12,12))
	DamageType FriendBullet
	Species "Marines"
}


ACTOR TimeInTheAir: Inventory {Inventory.MaxAmount 7}
ACTOR MakeTheNailDissapear: Inventory {Inventory.MaxAmount 1}