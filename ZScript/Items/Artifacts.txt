// Computer Area Map --------------------------------------------------------------

class BDP_AllMap : Inventory replaces Allmap
{
	Default
	{
		Inventory.MaxAmount 0;
		Inventory.PickupMessage "$GOTMAP";
		Inventory.PickupSound "items/computermap";
		BDP_AllMap.ScoreAmount 500;
		+COUNTITEM
		+BRIGHT
		+INVENTORY.ALWAYSPICKUP
		+INVENTORY.AUTOACTIVATE
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
		TNT1 A 0 NODELAY A_StartSound("COMPFAN", flags:CHANF_LOOPING, 0.25);
		PMAP ABCDCB 6;
		Loop;
	}
	
	override bool Use(bool pickup)
	{
		level.allmap = true;
		A_StopSound(1);
		if(GetCvar("bd_allmapdx") == 0)
			owner.giveinventory("BDP_PowerScanner", 1);
		return true;
	}
}

class BDP_PowerScanner : PowerScanner
{
	Default
	{
		Powerup.Duration 0x7FFFFFFD;
	}
}


// Invulnerability Sphere ---------------------------------------------------

class BDP_InvulnerabilitySphere  : PowerupGiver Replaces InvulnerabilitySphere
{
	Default
	{
		Inventory.MaxAmount 0;
		Powerup.Type "BDP_PowerInvulnerable";
		Inventory.PickupMessage "$GOTINVUL";
		Tag "Invulnerability";
		BDP_InvulnerabilitySphere.ScoreAmount 500;
		+COUNTITEM
		+FLOATBOB
		+FORCEXYBILLBOARD
		+BRIGHT
		+INVENTORY.AUTOACTIVATE
		+INVENTORY.ALWAYSPICKUP
		+INVENTORY.BIGPOWERUP
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
		PINV A 0 NoDelay A_StartSound("POWERHUM", flags:CHANF_LOOPING, 0.25);
	SpawnLoop:
		PINV AAABBBCCCDDD 2 A_SpawnItemEx("GreenFlareMedium", zofs:20);
		Loop;
	}
}

class BDP_PowerInvulnerable : PowerInvulnerable
{
	override void Tick()
	{
		if (Owner == NULL)
		{
			Destroy ();
		}
		If(effecttics == 104)
		{
			Owner.a_startsound("INVOFF",108);
		}
		If(owner && !owner.findinventory("pausepowerup") || owner && EffectTics < 105)
		{
			if (EffectTics == 0 || (EffectTics > 0 && --EffectTics == 0))
			{
				Destroy ();
			}
		}
	}
}

class MiniInvul : InvulnerabilitySphere
{
	Default
	{
		Tag "Invulnerability";
		Powerup.Duration -5;
		Powerup.Colormap 0.0, 0.0, 0.0, 1.0,1.0,1.0;
	}
}

// Soulsphere --------------------------------------------------------------

class BDP_Soulsphere : Inventory Replaces SoulSphere
{
	Default
	{
		Inventory.MaxAmount 0;
		Inventory.PickupMessage "$GOTSUPER";
		Inventory.PickupSound "items/soulsphere";
		Tag "Soulsphere";
		BDP_Soulsphere.ScoreAmount 500;
		BDP_Soulsphere.ExtraLives 1;
		+COUNTITEM
		+FLOATBOB
		+FORCEXYBILLBOARD
		+BRIGHT
		+INVENTORY.ALWAYSPICKUP
		+INVENTORY.AUTOACTIVATE
	}
	
	mixin BDP_ScoreMixin;
	
	override bool Use(bool pickup)
	{
		if(!(bdp_active_modifiers & Modifier_OneUpmanship)) owner.GiveBody(100, 200);
		return true;
	}
	
	States
	{
	Spawn:
		SOUL A 0 NoDelay A_startSound("POWERHUM", flags:CHANF_LOOPING, 0.25);
	SpawnLoop:
		SOUL AAABBBCCCDDDCCCBBB 2 A_SpawnItem("BlueFlareMedium2",0,20);
		Loop;
	}
}

// Megasphere --------------------------------------------------------------

class BDP_Megasphere : Inventory replaces Megasphere
{
	Default
	{
		+COUNTITEM
		+FLOATBOB
		+FORCEXYBILLBOARD
		+BRIGHT
		+INVENTORY.AUTOACTIVATE
		+INVENTORY.ALWAYSPICKUP
		Inventory.PickupMessage "$GOTMSPHERE";
		Inventory.PickupSound "items/soulsphere";
		Tag "Megasphere";
		BDP_Megasphere.ScoreAmount 500;
		BDP_Megasphere.ExtraLives 3;
	}
	
	mixin BDP_ScoreMixin;
	
	override bool Use(bool pickup)
	{
		if(!(bdp_active_modifiers & Modifier_OneUpmanship))
		{
			owner.GiveInventory("BDP_HeavyArmor", 1);
			owner.GiveBody(200, 200);
		}
		return true;
	}
	
	States
	{
	Spawn:
		TNT1 A 0 NoDelay A_StartSound("MEGAHUM", flags:CHANF_LOOPING, 0.25);
	SpawnLoop:
		MEGA AAABBBCCCDDD 2 A_SpawnItem("LensFlare",0,35);
		Loop;
	}
}	

// Light Amplification Visor --------------------------------------------------------------

class BDP_NightVision : Inventory replaces Infrared
{
	Default
	{
		+COUNTITEM
		+INVENTORY.ALWAYSPICKUP
		+INVENTORY.HUBPOWER
		Inventory.MaxAmount 1;
		Inventory.PickupMessage "$GOTVISOR";
		Inventory.PickupSound "items/nightvision";
		BDP_NightVision.ScoreAmount 500;
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
		PVIS E 7;
		PVIS D 3 Bright A_SpawnItemEx("RedFlareVerySmall", zofs:10);
		PVIS C 2 Bright A_SpawnItemEx("RedFlareVerySmall", zofs:10);
		PVIS BAB 1 Bright A_SpawnItemEx("RedFlareVerySmall", zofs:10);
		PVIS C 2 Bright A_SpawnItemEx("RedFlareVerySmall", zofs:10);
		PVIS D 3 Bright A_SpawnItemEx("RedFlareVerySmall", zofs:10);
		Loop;
	}
}

// Berserk --------------------------------------------------------------

/*class BDP_Berserk : Inventory replaces Berserk
{
	bool green;
	int spawnFrame;
	
	Default
	{
		+COUNTITEM
		+INVENTORY.AUTOACTIVATE
		+INVENTORY.ALWAYSPICKUP
		Inventory.PickupMessage "$GOTBERSERK";
		Inventory.PickupSound "misc/p_pkup";
		Inventory.PickupSound "GOTHPS";
		Tag "Berserk Pack";
		BDP_Berserk.ScoreAmount 500;
	}
	
	mixin BDP_ScoreMixin;
	
	override bool Use(bool pickup)
	{
		owner.GiveBody(100, 100);
		bool a;
		actor b;
		[a, b] = A_SpawnItemEx("BDP_BerserkEquipment", xvel:frandom(-1.0, 1.0), frandom(-1.0, 1.0), frandom(4.0, 8.0), flags:SXF_SETMASTER);
		let spawn = BDP_BerserkEquipment(b);
		if(!spawn) return true;
		spawn.frame = spawn.spawnFrame = spawnFrame;
		spawn.green = green;
		return true;
	}
	
	override void BeginPlay()
	{
		frame = random(0, 2);
		if(BDP_Handler.getMedikitColor())
		{
			frame += 3;
			green = true;
		}
		spawnFrame = frame;
		super.BeginPlay();
	}
	
	States
	{
	Spawn:
		PSTR # 2 NoDelay A_SpawnItemEx(green ? "GreenFlareSmall2" : "RedFlareVerySmall", zofs:8);
		Loop;
	}
}*/

// Partial Invisibility --------------------------------------------------------------

class BDP_Blursphere : PowerupGiver replaces Blursphere
{
	Default
	{
		+COUNTITEM
		+FLOATBOB
		+FORCEXYBILLBOARD
		+BRIGHT
		+VISIBILITYPULSE
		+INVENTORY.AUTOACTIVATE
		+INVENTORY.ALWAYSPICKUP
		+INVENTORY.BIGPOWERUP
		Inventory.PickupMessage "$GOTINVIS";
		powerup.type "BDP_PowerInvisibility";
		Powerup.Duration -30;
		RenderStyle "Translucent";
		Inventory.PickupSound "INVISON";
		BDP_Blursphere.ScoreAmount 500;
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
		TNT1 A 0 NoDelay A_StartSound("INVISHUM", flags:CHANF_LOOPING, 0.25);
	SpawnLoop:
		PINS AAABBBCCCDDD 2 A_SpawnItemEx("RedFlare", zofs:24);
		Loop;
	}
}

class BDP_PowerInvisibility : PowerInvisibility
{
	Default
	{
		+INVENTORY.NOSCREENBLINK;
	}
	Override void tick()
	{
		if (Owner == NULL)
		{
			Destroy ();
		}
		If(effecttics == 104)
		{
			Owner.a_startsound("BLURWARN",108);
		}
		If(effecttics == 15)
		{
			Owner.a_startsound("INVISOFF",107);
		}
		If(owner && !owner.findinventory("pausepowerup") || owner && EffectTics < 105)
		{
			if (EffectTics == 0 || (EffectTics > 0 && --EffectTics == 0))
			{
				
				Destroy ();
			}
		}
	}
	
	override void DoEffect ()
	{
		Super.DoEffect();
		Owner.A_SetRenderStyle(1.0,STYLE_ColorBlend);
	}
	
	override void AlterWeaponSprite (VisStyle vis, in out int changed)
	{
		
		if (changed == 1)
		{
			// something else set the weapon sprite back to opaque but this item is still active.
			float ts = float((Strength / 100) * (special1 + 1));
			vis.Alpha = clamp((1. - ts), 0., 1.);
			vis.RenderStyle = STYLE_ColorBlend;
		}
		// Handling of Strife-like cumulative invisibility powerups, the weapon itself shouldn't become invisible
		if ((vis.Alpha < 0.25f && special1 > 0) || (vis.Alpha == 0))
		{
			vis.Alpha = clamp((1. - Strength/100.), 0., 1.);
			vis.invert = true;
		}
		changed = -1;	// This item is valid so another one shouldn't reset the translucency
	}
}

class SalInvisibleActor : Actor
{
	Default
	{
		+NONSHOOTABLE
		+NODAMAGE
		+THRUACTORS
		+NOTELEPORT
		+DROPOFF
		+isMonster;
		//RenderStyle "None";
	}
	States
	{
		Spawn:
			TNT1 A -1;
			WAIT;
	}
}

class SalInvisHandler : EventHandler
{
	Array <Actor> monsterList;
	SalInvisibleActor playerInvisTarget[MAXPLAYERS];

	void SalInvisTargetStatus(int i)
	{
		let mo = players[i].mo;

		if (mo)
		{
			let targ = playerInvisTarget[i];

			if (targ && mo.bShadow == false)
			{
				// Destroy it if it exists
				targ.Destroy();
			}
		}
	}

	void SalMoveInvisTarget(int i)
	{
		let mo = players[i].mo;

		if (mo)
		{
			let targ = playerInvisTarget[i];

			if (mo.bShadow == true)
			{
				if (targ)
				{
					// Move the existing one directly to your current position
					targ.SetOrigin(mo.pos, false);
				}
				else
				{
					// Create a new invisible target spot at your location
					playerInvisTarget[i] = SalInvisibleActor(mo.Spawn("SalInvisibleActor", mo.pos, NO_REPLACE));
				}
			}
		}
	}

	override void WorldThingSpawned(WorldEvent e)
    {
		if (e.Thing.bIsMonster)
		{
			// Store in a list for later.
			monsterList.push(e.Thing);
		}
	}

	override void WorldTick()
	{
		Array <Actor> removalList;

		for (int i = 0; i < MAXPLAYERS; i++)
		{
			let target = playerInvisTarget[i];

			if (!playeringame[i])
			{
				if (target)
				{
					target.Destroy();
				}
				continue;
			}
			else
			{
				let mo = players[i].mo;

				if (!target)
				{
					SalMoveInvisTarget(i);
				}
				else
				{
					SalInvisTargetStatus(i);

					if (mo.CurState == mo.ResolveState("Missile")
						|| mo.CurState == mo.ResolveState("Melee")
						|| mo.CurState == mo.ResolveState("Pain")
						|| mo.findinventory("moveblurspheretarget"))
					{
						SalMoveInvisTarget(i);
						mo.takeinventory("moveblurspheretarget",1);
					}
				}
			}
		}

		for (int i = 0; i < monsterList.Size(); i++)
        {
            Actor mo = monsterList[i];

			if (!mo || mo.health <= 0)
			{
				// Must be removed later, because we can't change iteration length.
				removalList.push(mo);
				continue;
            }

			if (mo.bSeeInvisible)
			{
				// Don't remove, but don't do the target setting either
				continue;
			}
			else if (mo.bmissile)
			{
				// Don't remove, but don't do the target setting either
				continue;
			}
			else
			{
				let target = PlayerPawn(mo.target);

				if (target)
				{
					let n = target.PlayerNumber();
					let invisTarget = playerInvisTarget[n];

					if (invisTarget)
					{
						// Your target has a SalInvisTarget,
						// so you need to change your target to it immediately.
						mo.target = invisTarget;
					}
				}
			}
        }

		for (int i = 0; i < removalList.Size(); i++)
        {
            int j = monsterList.Find(removalList[i]);
            monsterList.Delete(j);
        }
	}
}

// Quad Damage --------------------------------------------------------------

class BDP_QuadDamage : PowerupGiver
{
	Default
	{
		+COUNTITEM
		+FLOATBOB
		+INVENTORY.ALWAYSPICKUP
		+INVENTORY.AUTOACTIVATE
		Inventory.PickupMessage "Quad Damage!";
		Inventory.PickupSound "sigil/quake";
		Tag "Quad Damage";
		powerup.duration -30;
		powerup.type "BDP_PowerQuadDamage";
		BDP_QuadDamage.ScoreAmount 500;
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
	    QUAK A 0 NoDelay A_StartSound("QUADHUM", flags:CHANF_LOOPING, 0.25);
	SpawnLoop:
		QUAK ABCB 6 Bright;
		Loop;
	}
}

class BDP_PowerQuadDamage : PowerDamage
{
	Default
	{
		damagefactor "normal", 4;
	}
	Override void tick()
	{
		if (Owner == NULL)
		{
			Destroy ();
		}
		
		If(EffectTics == 104)
		{
			Owner.a_startsound("items/quaddamage/warn",107);
		}
		
		If(owner && !owner.findinventory("pausepowerup") || owner && EffectTics < 105)
		{
			if (EffectTics == 0 || (EffectTics > 0 && --EffectTics == 0))
			{
				Destroy ();
			}
		}
	}
}

// Haste Sphere --------------------------------------------------------------

class BDP_HasteSphere : PowerUpGiver
{
	Default
	{
		+COUNTITEM
		+FLOATBOB
		+FORCEXYBILLBOARD
		+BRIGHT
		+INVENTORY.AUTOACTIVATE
		+INVENTORY.ALWAYSPICKUP
		Inventory.PickupMessage "Haste!";
		Inventory.PickupSound "sigil/haste";
		Tag "Haste";
		powerup.duration -30;
		powerup.type "BDP_PowerSpeed";
		BDP_HasteSphere.ScoreAmount 500;
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
	    TURB A 0 NoDelay A_StartSound("POWERHUM", flags:CHANF_LOOPING, 0.25);
	SpawnLoop:
		TURB AAABBBCCCDDD 2 A_SpawnItemEx("YellowFlareSmall", zofs:26);
		Loop;
	}
}

class BDP_PowerSpeed : powerspeed
{
	Default
	{
		Speed 1.7;
		inventory.icon "TNT1A0";
		+INVENTORY.NOTELEPORTFREEZE
	}
	override void Tick()
	{
		if (Owner == NULL)
		{
			Destroy ();
		}
		If(effecttics == 104)
		{
			Owner.a_startsound("HASTEOFF",108);
		}
		If(owner && !owner.findinventory("pausepowerup") || owner && EffectTics < 105)
		{
			if (EffectTics == 0 || (EffectTics > 0 && --EffectTics == 0))
			{
				Destroy ();
			}
		}
	}
}

// Radiation Suit --------------------------------------------------------------

class BDP_RadSuit : PowerupGiver replaces RadSuit
{
	Default
	{
		Height 46;
		+INVENTORY.AUTOACTIVATE
		+INVENTORY.ALWAYSPICKUP
		+BRIGHT
		Inventory.MaxAmount 0;
		Inventory.PickupMessage "$GOTSUIT" ;
		Inventory.PickupSound "items/radsuit";
		Powerup.Type "PowerIronFeet";
		Powerup.Color "None";
		BDP_RadSuit.ScoreAmount 500;
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
		SUIT A -1;
		Stop;
	}
}

// Rage Sphere --------------------------------------------------------------

class BDP_RageSphere : PowerupGiver
{
	Default
	{
		+COUNTITEM
		+FLOATBOB
		+FORCEXYBILLBOARD
		+BRIGHT
		+INVENTORY.AUTOACTIVATE
		+INVENTORY.ALWAYSPICKUP
		+INVENTORY.BIGPOWERUP
		Inventory.MaxAmount 0;
		Powerup.Type "BDP_PowerRage";
		Inventory.PickupMessage "Rage sphere! Liquify them.";
		Tag "Rage Sphere";
		Inventory.PickupSound "RAGEPUP";
		BDP_RageSphere.ScoreAmount 500;
	}
	
	mixin BDP_ScoreMixin;
	
	States
	{
	Spawn:
		DDMG A 0 NoDelay A_StartSound("POWERHUM", flags:CHANF_LOOPING, 0.25);
	SpawnLoop:
		DDMG AAABBBCCCDDD 2 A_SpawnItemEx("RedFlareMedium", zofs:20);
		Loop;
	}
}

class BDP_PowerRage : PowerSpeed
{
	Default
	{
		Powerup.Duration -30;
		Speed 1.5;
		+INVENTORY.HUBPOWER
	}
	override void initeffect()
	{
		owner.giveinventory("PowerStrength",1);
		Super.initeffect();
	}
	Override void tick()
	{
		if (Owner == NULL)
		{
			Destroy ();
		}
		//console.printf("%i",EffectTics);
		If(effecttics == 104)
		{
			Owner.a_startsound("BRSKWARN",108);
		}
		If(owner && !owner.findinventory("pausepowerup") || owner && EffectTics < 105)
		{
			if (EffectTics == 0 || (EffectTics > 0 && --EffectTics == 0))
			{
			Destroy ();
			}
		}
	}
	override void doeffect()
	{
		If(owner)
		{
			owner.A_selectweapon("BDP_Fist");
		}
		Super.doeffect();
	}
	
	override void ModifyDamage (int damage, Name damageType, out int newdamage, bool passive, Actor inflictor, Actor source, int flags)
	{
		If(passive)
		{
			newdamage = (damage * 0.25);
		}
		Else If(owner && damage > 1000)
		{
			Owner.givebody(10);
			newdamage = damage;
			source.a_startsound("Items/Rage/Kill",624);
		}
	}
}

class BDP_BarrelBonus : RandomSpawner
{
    Default
    {
        DropItem "BDP_HealthBonus", 255, 10;
        DropItem "BDP_ArmorBonus", 255, 10;
        DropItem "BDP_Stimpack", 255, 10;
        DropItem "BDP_SmallBulletBox", 255, 10;
		DropItem "BDP_Shell", 255, 10;
		DropItem "BDP_RocketAmmo", 255, 10;
		DropItem "BDP_Revolver", 255, 1;
		DropItem "BDP_SubmachineGun", 255, 1;
    }
}