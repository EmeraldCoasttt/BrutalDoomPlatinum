class BDPPlasmaRifle : BDPWeaponBase
{
	action state A_LowSprites(string nosprite, string medsprite, string highsprite, statelabel lowstate = null)
	{
		let psp = player.FindPSprite(OverlayID());
		if(psp)
		{
			if(invoker.mag < 1 || invoker.mag < 11 && !lowstate)
			{
				psp.sprite = GetSpriteIndex(nosprite);
			}
			else if(invoker.mag < 11 && lowstate)
			{
				return resolvestate(lowstate);
			}
			else if(invoker.mag < 31)
			{
				psp.sprite = GetSpriteIndex(medsprite);
			}
			else
			{
				psp.sprite = GetSpriteIndex(highsprite);
			}
		}
		return resolvestate(null);
	}
	
	action state A_LowFrame(int noframe = -1, int medframe = -1, int highframe = -1, statelabel lowstate = null)
	{
		let psp = player.FindPSprite(OverlayID());
		if(psp)
		{
			if(noframe == -1)
			{
				noframe = psp.frame + 10;
			}
			if(medframe == -1)
			{
				medframe = psp.frame + 5;
			}
			if(highframe == -1)
			{
				highframe = psp.frame;
			}
			if(invoker.mag < 1 || invoker.mag < 11 && !lowstate)
			{
				psp.frame = noframe;
			}
			else if(invoker.mag < 11 && lowstate)
			{
				return resolvestate(lowstate);
			}
			else if(invoker.mag < 31)
			{
				psp.frame = medframe;
			}
			else
			{
				psp.frame = highframe;
			}
		}
		return resolvestate(null);
	}
	
	int heat;
	
	Default
	{
		Weapon.SlotNumber 6;
		Weapon.SlotPriority 3;
		Weapon.SelectionOrder 100;
		Weapon.AmmoGive 40;
		Inventory.PickupSound "PLSDRAW";
		Weapon.AmmoType "BDPCell";
		BDPWeaponBase.MagCapacity 50;
		Tag "$BDPPLASMA";
		Inventory.PickupMessage "You got the Plasma Rifle! (Slot 6)";
		inventory.althudicon "PRASA0";
		inventory.maxamount 2;
		BDPWeaponBase.DualWeapon "BDPDualPlasmaRifle";
	}
	States
	{
	CacheSprites:
		PLSN ABCDE 0;
		2LSN KLMNOPQRST 0;
		2LSI ABCDE 0;
		3LSN KLMNOPQRST 0;
		3LSI ABCDE 0;
		4LSN KLMNOPQRST 0;
		4LSI ABCDE 0;
		3LSR AB 0;
		Stop;
	Grenade: //30
		PRSG PQRS 1;
		TNT1 A 22;
		PRSG WVUT 1;
		Goto Ready;
	Kick: //18
		PRSK ABCD 1 A_LowFrame();
		PRSK E 10 A_LowFrame();
		PRSK DCBA 1 A_LowFrame();
		Goto Ready;
	SlideKick: //25
		PRSK ABCD 1 A_LowFrame();
		PRSK E 17 A_LowFrame();
		PRSK DCBA 1 A_LowFrame();
		Goto Ready;
	Taunt: //30
		PRSK ABCD 1 A_LowFrame();
		PRSK E 22 A_LowFrame();
		PRSK DCBA 1 A_LowFrame();
		Goto Ready;
	Sprint:
		PRSK ABCD 1
		{
			A_SetSprites("TNT1A0",1);
			A_StopSound(4);
			A_LowFrame();
		}
	SprintLoop:
		PRSK E 1
		{
			A_SetSprites("TNT1A0",1);
			A_StopSound(4);
			A_LowFrame();
		}
		Loop;
	SprintEnd:
		PRSK DCBA 1
		{
			A_SetSprites("TNT1A0",1);
			A_StopSound(4);
			A_LowFrame();
		}
		Stop;
	Ready:
		TNT1 A 0 {
			A_ClearOverlays(6,6);
			invoker.heat = 0;
			return A_LowFrame(5,7,0,"ReadyLowAmmo");
		}
		PRSG # 1 A_WeaponReadyDX(WRF_ALLOWRELOAD);
		Loop;
	ReadyLowAmmo:
		PRSG GGGGGGFFFFFF 1 A_WeaponReadyDX(WRF_ALLOWRELOAD);
		Loop;
	Deselect:
		PRSG PQRS 1;
		TNT1 A 1;
		TNT1 A 0 A_Lower();
		Wait;
	DeselectSecondGun:
		DUPL YXWV 1;
		stop;
	Select:
		TNT1 A 1 {A_SetCrosshairDX("PLASRet", 1000); A_GunslingerMag();}
		TNT1 A 0 A_StartSound("PLSDRAW");
		PRSG WVUT 1;
		Goto Ready;
	SelectFromDual:
		TNT1 A 0 {A_SetCrosshairDX("PLASRet", 1000); A_StartSound("foley/PlasmaInspectMisc",10);}
		TNT1 A 0 A_OverlayFlags(6,PSPF_ADDWEAPON|PSPF_ADDBOB|PSPF_FLIP|PSPF_MIRROR,True);
		TNT1 A 0 A_Overlay(2,"DeselectSecondGun");
		DUPR YXWVUTSR 1;
		Goto Ready;
	MuzzleFlash:
		PLSM AB 1 Bright;
		Stop;
	Fire:
		PLSN A 0 A_CheckIfMag();
		PRSG B 1 Bright
		{
			A_FireProjectileDX("Plasma_Ball", frandom(0.3, -0.3), frandom(-0.3, 0.3), 0, 0, -8, false);
			A_FireProjectile ("BluePlasmaParticleWeapon", random(-11, 11), 0, -1, -3, 0, random(-7, 14));
			A_FireProjectile ("BluePlasmaParticleWeapon", random(-11, 11), 0, -1, -3, 0, random(-7, 14));
			A_FirePitchDown("PLSM9", 1, 15);
			A_FlashOverlay();
			A_WeaponRecoil(0.3);
			A_AlertMonstersDX();
			A_ZoomFactor(0.99);
			A_QuadSound();
			A_GunLight(500,2,0,128,255);
			A_TakeMag();
		}
		PRSG D 1 Bright
		{
			A_GunLight(500,2,0,128,255);
			invoker.heat++;
			A_ZoomFactor(1.0);
		}
		PRSG E 1 A_WeaponOffset(random(-1,1),32+random(0,1));
		PRSG F 1;
		TNT1 A 0 A_WeaponOffset(0,32);
		TNT1 A 0 A_RefireDX();
		TNT1 A 0 A_JumpIf(invoker.heat >= 5,"Cooldown");
		TNT1 A 0 {invoker.heat = 0;}
		Goto Ready;
	Cooldown:
		TNT1 A 0 {invoker.heat = 0;}
		TNT1 A 0 A_StartSound("PLSCOOL", 3);
		PRSA CDEFGH 1;
		PRSA I 15 A_FireProjectile("SmokeSpawner11",0,0,-10,16);
		PRSA HGFEDD 1;
		Goto Ready;
// Charging up secondary ------------------------------------------------------------
	AltFlash:
		PLSM IJKL 1 Bright;
		Stop;
	AltFire:
		PLSN A 0 A_CheckIfMag(10);
		TNT1 A 0 A_SetCrosshairDX("PLS2Ret", 500);
		PLSN A 0 A_StartSound("PLSCHARG",1);
		PRSG II 4
		{
			A_TakeMag();
			invoker.heat++;
		}
	AltFireLoop:
		TNT1 A 0 A_JumpIf(invoker.heat >= 5,"AltLoop2");
		TNT1 A 0
		{
			A_FlashOverlay(state:"AltFlash");
			A_TakeMag();
			invoker.heat++;
			return A_JumpIf(invoker.heat >= 10,"ContinueAlt");
		}
		PRSG JKJK 1 Bright A_GunLight(200,2,0,128,255);
		TNT1 A 0
		{
			if(player.cmd.buttons & BT_RELOAD)
			{
				return resolvestate("CancelAltfire");
			}
			else
			{
				return resolvestate(null);
			}
		}
		Loop;
	CancelAltfire:
		TNT1 A 0
		{
			A_StartSound("PLSCOOL", 1);
			A_StopSound(6);
		}
	CancelAltLoop:
		TNT1 A 0 A_JumpIf(invoker.heat == 0,"EndAlt");
		PRSG I 2
		{
			A_TakeMag(-1);
			invoker.heat--;
		}
		loop;
	EndAlt:
		TNT1 A 0 A_SetCrosshairDX("PLASRet", 1000);
		Goto Ready;

// Holding Charge ------------------------------------------------------------
	HoldAltFlash:
		PLSM MNOP 1 Bright;
		Stop;
	ContinueAlt:
		TNT1 A 0 {
			A_FlashOverlay(state:"HoldAltFlash");
			A_StartSound("PLSFULL", 6, CHANF_LOOPING, 1, 0.8);
		}
		PRSG LMNO 1
		{
			A_GunLight(300,2,0,128,255);
			if(player.cmd.buttons & BT_RELOAD)
			{
				return resolvestate("CancelAltfire");
			}
			else if(!(player.cmd.buttons & BT_ALTATTACK))
			{
				return resolvestate("AltBlast");
			}
			else
			{
				return resolvestate(null);
			}
		}
		Loop;
	
// Let Fly --------------------------------------------------------------------------
	AltBlast:
		PRSG B 1 {
			invoker.heat = 0;
			for(int i = 0; i < 13; i++)
				A_FireProjectile("PlasmaBall75", frandom(-8,8), 0, 0, -5, 0, random(-2,2));
			for(int i = 0; i < 9; i++)
				A_FireProjectile ("BluePlasmaParticleWeapon", random(-19,19), 0, -1, 0, 0, random(-9,9));
			A_FlashOverlay();
			A_StopSound(6);
			A_ZoomFactor(0.9);
			A_ClearOverlays(6,6);
			A_Recoil(3*cos(pitch));
			if(vel.z != 0)
				ThrustThingZ(0,pitch/2,0,1);
			else
				ThrustThingZ(0,pitch/3,0,1);
			A_QuadSound();
			A_FireProjectile("PlasmaFlareSpawn",-5,0,0,0);
			A_StartSound("PLSALTFR", 1);
			A_StartSound("PLSM9", 2);
			A_WeaponRecoil(8);
			A_GunLight(500,2,0,128,255);
		}
		PRSG C 1 {
			A_GunLight(500,2,0,128,255);
			A_ZoomFactor(1.0);
		}
		PRSA AB 1;
		TNT1 A 0 A_StartSound("PLSCOOL", 3);
		PRSA CDEFGH 1;
		PRSA IIIIII 4 A_FireProjectile("SmokeSpawner11",0,0,-10,16);
		PRSA HGFEDD 1;
		TNT1 A 0 A_SetCrosshairDX("PLASRet", 1000);
		Goto Ready;
	Spawn:
		PRAS A -1;
		Stop;
	NoAmmo:
		TNT1 A 0 A_Dryfire("BEPBEP");
		Goto Ready;
	DoReload:
		TNT1 A 0
		{
			A_ClearReFire();
			A_StartSound("Plasma/Reload1", 5);
			A_DiscardMag();
			if(invoker.mag == 0)
			{
				A_FireProjectile("PlasmaCaseSpawn",-225,0,25,-15,-14);
			}
		}
		PRSR AAZBCDEFGHH 1;
		PRSR I 10;
		TNT1 A 0 A_StartSound("foley/PlasmaInspectMisc", 10);
		PRSR JKLMNNO 1;
		TNT1 A 0 A_StartSound("Plasma/Reload2", 5);
		PRSR OOOO 1;
		TNT1 A 0 A_ReloadMag();
		PRSR PQRSTTTTUVWXY 1;
		PRSR CZAA 1;
		PRSG IFI 2;
		Goto Ready;
	SpecialAction:
		TNT1 A 0 A_DualWield();
		Goto Ready;
	/*Swapweaponspecials:
	TNT1 A 0 A_takeinventory("swapriflespecial",1)
	3LSI C 0 A_CheckIfMag(11,3)
	2LSI B 0 A_CheckIfMag(31,2)
	PLSI A 0
	#### A 0
	#### ABCDE 1
	#### A 0 
	{
	if(Countinv("NeedlerSelected")==1)
			{
			A_Print("\cyCharge altfire",2);
			//A_SetCVAR("bd_needlegun",false);
			A_StartSound("BEPBEP");
			A_takeinventory("needlerselected",1);
			}
		else
			{
			A_Print("\cgNeedlegun altfire",2);
			//A_SetCVAR("bd_needleGun",true);
			A_StartSound("NDLRON");
			A_giveinventory("needlerselected",1);
			}
	}
	PLSI FGG 1
	PLSI HIIIF 1
	3LSI C 0 A_CheckIfMag(11,3)
	2LSI B 0 A_CheckIfMag(31,2)
	PLSI A 0
	#### A 0
	#### EDCBA 1
	Goto ready
	Needlegun:
		//PLSN A 0 A_CheckReload
		PLSN A 0 A_JumpIfInventory("Reloading",1,"Reload")
		PLSN A 0 A_CheckIfAmmo("PlasmaAmmo")
		TNT1 AAAA 0
		TNT1 A 0 A_GunLight(500,2,255,0,225)
		#### A 0 A_QuadSound
		PLSN A 0 A_StartSound("NDLRFIRE", 1)
		//PLSN A 0 A_FireProjectile("PlasmaFlareSpawn", 0, 0, 0, 0)
		PLSN A 0 A_TakeAmmo("DoublePlasmaAmmo", 1)
		PLSN A 0 A_JumpIfInventory("DMgame", 1, "FireDM")
		PLSN A 0 A_ZoomFactor(0.99)
		PLSN A 0 A_SetPitch(pitch-0.3, SPF_INTERPOLATE)
		//TNT1 A 0 A_FireProjectile ("PlasmaFlareSpawn", 0, 0, 0, 2)
		//TNT1 AA 0 A_FireProjectile ("BluePlasmaParticleWeapon", random(-11,11), 0, -1, -3, 0, random(-7,14))
		"----" A 0 { If(GetCVAR("BD_MagGoClicky")==1) { if(CountInv("PlasmaAmmo")<=8) { A_StartSound("MagClick", 7); }}}
		PLSF E 1 BRIGHT A_FireProjectile("Needle", frandom(3,-3), 1, 0, -14, 0, frandom(3,-3))
		TNT1 A 0 A_GiveInventory("PlasmaCooldowncounter",1)
		PLSN A 0 A_TakeAmmo("PlasmaAmmo",1)
		PLSN A 0 A_ZoomFactor(1.0)
		PLSN A 0 A_SetPitch(pitch+0.3, SPF_INTERPOLATE)
		PLSF F 1 BRIGHT
		#### A 0 A_WeaponOffset(random(-1,1),32+random(0,1) )
		
		//PLST C 1
		PLSF G 1
		#### A 0 A_WeaponOffset(0,32)
		TNT1 A 0 A_ReFire
		TNT1 A 0 A_JumpIfInventory("PlasmaCooldowncounter",15,"Cooldown")
		TNT1 A 0 A_TakeInventory("PlasmaCooldowncounter",15)
		PLSF H 1
		Goto Ready*/
	}
}

class FlareVT : VisualThinker
{
	int lifetime;
	
	override void PostBeginPlay()
	{
		Super.PostBeginPlay();
		flags = SPF_FULLBRIGHT;
		SetRenderStyle(STYLE_Add);
	}
	
	override void Tick()
	{
		if(lifetime < 1)
		{
			Destroy();
		}
		lifetime--;
		Super.Tick();
	}
}

class BlueFlareVT : FlareVT
{
	override void PostBeginPlay()
	{
		Super.PostBeginPlay();
		texture = TexMan.CheckForTexture(random(0,1) == 1 ? 'LENBA0' : 'LENBB0');
		scale = (0.4,0.4);
		alpha = 0.4;
	}
}

class BlueFlareSmallVT : FlareVT
{
	override void PostBeginPlay()
	{
		Super.PostBeginPlay();
		texture = TexMan.CheckForTexture(random(0,1) == 1 ? 'LENBA0' : 'LENBB0');
		scale = (0.13,0.13);
		alpha = 0.3;
	}
}


class Plasma_Ball : FastProjectile
{
	action void A_SpawnFlare(class<VisualThinker> flare, int lifetime)
	{
		let thonk = FlareVT(level.SpawnVisualThinker(flare));
		if(thonk)
		{
			thonk.pos = self.pos - self.vel;
			thonk.vel = self.vel;
			thonk.lifetime = lifetime;
		}
	}
	
	Default
	{
		Radius 10;
		Height 2;
		Speed 60;
		DamageFunction 40;
		DamageType 'Plasma';
		Decal "SmallerScorch";
		Projectile;
		+RANDOMIZE
		+SPECTRAL
		-NOBLOCKMAP
		+NOBLOOD
		+NORADIUSDMG
		+THRUSPECIES
		+MTHRUSPECIES
		+FORCEXYBILLBOARD
		Species "Marines";
		Health 5;
		Scale 1.0;
		renderstyle 'ADD';
		alpha 0.99;
		DeathSound "weapons/plasmax";
		Obituary "$OB_MPPLASMARIFLE";
	}
	States
	{
	Spawn:
		PLSS AB 1 Bright;
	Fly:
		PLSS AB 1 Bright A_SpawnFlare("BlueFlareSmallVT",1);
		Loop;
	Death:
		PLSN A 0 A_CustomMissile ("BluePlasmaFire", 0, 0, random (0, 360), 2, random (0, 360));
		TNT1 AAAAA 0 A_CustomMissile ("BluePlasmaParticle", 0, 0, random (0, 360), 2, random (0, 360));
		TNT1 A 1; //A_Explode(5,50,0)
		PLSE ABC 2 Bright A_SpawnFlare("BlueFlareVT",2);
		PLSE DE 2 Bright A_SpawnFlare("BlueFlareVT",2);
		TNT2 AAA 9 A_CustomMissile ("PlasmaSmoke", 1, 0, random (0, 360), 2, random (0, 160));
		Stop;
	XDeath:
		PLSN A 0 A_CustomMissile ("BluePlasmaFire", 0, 0, random (0, 360), 2, random (0, 360));
		TNT1 AAAAA 0 A_CustomMissile ("BluePlasmaParticle", 0, 0, random (0, 360), 2, random (0, 360));
		TNT1 A 1 A_Explode(5,50,0);
		PLSE ABC 2 Bright A_SpawnFlare("BlueFlareVT",2);
		PLSE DE 2 Bright A_SpawnFlare("BlueFlareVT",2);
		Stop;
	}
}


class PlasmaBall75 : Plasma_Ball
{
	Default
	{
		DamageFunction 50;
	}
}