class BDP_WeaponDropper : Actor
{
	name weaponToDrop;
	property WeaponToDrop : weaponToDrop;
	
	Default
	{
		Radius 0;
		Height 0;
		+THRUACTORS
	}
	States
	{
		Spawn:
		TNT1 A 0 NoDelay
		{
			if(random(0,9) == 0)
			{
				if(random(0,1) == 0)
				{
					A_CustomMissile("BDP_DroppedFragGrenade", 20, 0, random (0, 360), 2, random (30, 50));
				}
				else
				{
					A_CustomMissile("BDP_DroppedPipeBomb", 20, 0, random (0, 360), 2, random (30, 50));
				}
			}
			A_CustomMissile(WeaponToDrop, 20, 0, random (0, 360), 2, random (30, 50));
		}
		Stop;
	}
}

class BDP_DroppedWeapon : Actor
{
	Default
	{
		speed 6;
		health 1;
		radius 16;
		height 4;
		Gravity 0.9;
		damage 0;
		XScale 0.7;
		YScale 0.8;
		+ROLLSPRITE
		+ROLLCENTER
		+THRUACTORS
		+MISSILE
		+NOTELEPORT
		+NOBLOCKMAP
		+BLOODLESSIMPACT 
		+FORCEXYBILLBOARD
		+NODAMAGETHRUST
		+MOVEWITHSECTOR
		Mass 1;
		BounceType "Doom";
		BounceFactor 0.4;
		WallBounceFactor 0.2;
	}
	action void A_SpawnDroppedWeapon(name weapontype, int ammonum)
	{
		Weapon weap2drop = Weapon(Spawn(weapontype,pos));
		if (weap2drop)
		{
			weap2drop.ammoGive1 = ammonum;
		}
	}
}
class BDP_PistolDrop : BDP_WeaponDropper {Default {BDP_WeaponDropper.WeaponToDrop "DroppedPistol";}}
class BDP_RifleDrop : BDP_WeaponDropper {Default {BDP_WeaponDropper.WeaponToDrop "DroppedRifle";}}
class BDP_ShotgunDrop : BDP_WeaponDropper { Default {BDP_WeaponDropper.WeaponToDrop "DroppedShotgun";}}
class BDP_SMGDrop : BDP_WeaponDropper {Default {BDP_WeaponDropper.WeaponToDrop "DroppedSMG";}}
class BDP_MinigunDrop : BDP_WeaponDropper {Default {BDP_WeaponDropper.WeaponToDrop "DroppedMinigun";}}

class BDP_DroppedRifle : BDP_DroppedWeapon
{
	States
	{
	Spawn:
		RIFL A 1 {A_SetRoll(roll + 11.25);bThruActors = 0;}
		Loop;
	Death:
		TNT1 A 0 A_SpawnDroppedWeapon("BDP_AssaultRifle",15);
		Stop;
	}
}

class BDP_DroppedMinigun : BDP_DroppedWeapon
{
	States
	{
	Spawn:
		MGUN A 1 {A_SetRoll(roll + 11.25);bThruActors = 0;}
		Loop;
	Death:
		TNT1 A 0 A_SpawnDroppedWeapon("BDP_Minigun",30);
		Stop;
	}
}

class BDP_DroppedShotgun : BDP_DroppedWeapon 
{
	States
	{
	Spawn:
		SHTC A 1 {A_SetRoll(roll + 11.25);bThruActors = 0;}
		Loop;
	Death:
		TNT1 A 0 A_SpawnDroppedWeapon("BDP_Shotgun",4);
		Stop;
	}
}

class BDP_DroppedPistol : BDP_DroppedWeapon
{
	States
	{
	Spawn:
		PIST G 1 {A_SetRoll(roll + 11.25);bThruActors = 0;}
		Loop;
	Death:
		TNT1 A 0 A_SpawnDroppedWeapon("BDP_Pistol",7);
		Stop;
	}
}

class BDP_DroppedSubmachineGun : BDP_DroppedWeapon
{	
	States
	{
	Spawn:
		SMGZ A 1 {A_SetRoll(roll + 11.25);bThruActors = 0;}
		Loop;
	Death:
		TNT1 A 0 A_SpawnDroppedWeapon("BDP_SubmachineGun",7);
		Stop;
	}
}

class BDP_DroppedFragGrenade : BDP_DroppedWeapon
{
	Default
	{
		Scale 0.5;
		Speed 9;
		BounceSound "HandNadeBounce";
	}
	States
	{
	Spawn:
		TNT1 A 0 NoDelay A_Jump(256,"Spawn1","spawn2");
	Spawn1:
		GRND D 1 {A_SetRoll(roll + 11.25);bThruActors = 0;}
		Loop;
	Spawn2:
		GRND D 1 {A_SetRoll(roll - 11.25);bThruActors = 0;}
		Loop;
	Death:
		TNT1 A 0 A_SpawnItemEx("AmmoFragGrenade");
		Stop;
	}
}

class BDP_DroppedPipeBomb : BDP_DroppedWeapon
{
	Default
	{
		Scale 0.4;
		Speed 9;
		BounceSound "PIPBOING";
	}
	States
	{
	Spawn:
		TNT1 A 0 NoDelay A_Jump(256,"Spawn1","Spawn2");
	Spawn1:
		PIPB B 1 {A_SetRoll(roll + 11.25);bThruActors = 0;}
		Loop;
	Spawn2:
		PIPB B 1 {A_SetRoll(roll - 11.25);bThruActors = 0;}
		Loop;
	Death:
		TNT1 A 0 A_SpawnItemEx("AmmoPipeBomb");
		Stop;
	}
}